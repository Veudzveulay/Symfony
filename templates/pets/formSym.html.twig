{% extends 'base.html.twig' %}
    {% block body %}

        <div class="container">

            <form action="{{ path('app_pets_createForms') }}" method="post">

            <h1> Création de votre Pet </h1>

            {{ form_start(form) }}

                {{ form_widget(form) }}

            {{ form_end(form) }}
            </form>
    </div>

    {% endblock %}